import 'package:flutter/material.dart';

class ArticsPage extends StatelessWidget {
  const ArticsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Artics Page"),
    );
  }
}
